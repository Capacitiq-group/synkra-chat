# Handover — 20 Sep 2026

## Where things stand right now

This session ran long and got messy — starting fresh next session
rather than continuing this one.

**Staging deploy status is unclear.** Refilwe ran the deploy, but:
- The expected migrations were not visible in her terminal output
- Features built this session are not showing up on her test account

This needs actually investigating next session (check what commit is
live, whether migrations actually ran, whether the build even picked
up the latest code) — nothing here has been diagnosed yet, just
flagged as broken/uncertain.

## Production — what we learned this session

Confirmed via screenshots of the Coolify Docker Compose config for
production's `service-nkkk2xwmhykdaut9uj9rq8ca` (project "SYNKRA
Chat" → environment "production"):

- Rails and Sidekiq are both running the **public
  `chatwoot/chatwoot:latest-ce` image straight from Docker Hub** — no
  `build:` directive anywhere in the compose file. None of Synkra's
  custom code (this session's or any earlier session's work) is
  running on production.
- `chatwoot_production`'s database has nothing worth preserving —
  confirmed safe to treat as empty.
- Server: same VPS as staging (167.86.106.152), Coolify-managed,
  domain chat.synkra.co.za already pointed at it.

**Decided:** production will be set up to match staging's exact
pattern — a manual git checkout on the server (like staging's
`/opt/synkra-chat-dev`), built with `docker build`, referenced by
image tag in the compose file. Not a Coolify-native `build:` block.

**Not yet done:** don't know if a checkout already exists on the
server for production (something like `/opt/synkra-chat-prod`) —
needs checking via Coolify's Terminal next session before writing
exact commands.

## Next session should start with

1. Diagnose the staging deploy — what's actually live, why migrations/
   features aren't showing
2. Check the production server for an existing checkout directory
3. Once both are clear, write the actual production setup steps
   (checkout, build, migrate, reconcile config, env vars including
   live Paystack keys, deploy, verify)

## Reference docs already in the repo (still accurate)

- `docs/synkra/deployment-runbook-2026-09-16-v4.md` — full staging
  runbook, 8 migrations, everything built this session
- `docs/synkra/help-center/` — 21-page Q&A docs site content
- `docs/synkra/marketing-transactional-email-split.md`,
  `docs/synkra/resend-email-setup.md` — as titled
